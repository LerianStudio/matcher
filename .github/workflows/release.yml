name: Release

on:
  push:
    branches:
      - develop
      - release-candidate
      - main
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - '.github/**'
      - 'LICENSE'
      - 'CODEOWNERS'
      - '.gitignore'
      - 'images/**'
      - '*.txt'

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    name: Create Release
    uses: LerianStudio/github-actions-shared-workflows/.github/workflows/release.yml@3197048995e33cfe9880a4c2c76c20f4cf9bd3ef # v1.26.3
    with:
      runner_type: "blacksmith-4vcpu-ubuntu-2404"
    secrets: inherit
